# Changelog

本文件记录 `秋招信息工作台（共享版）.html` 及配套模板的公开版本变化。
（作者本地还有一套私有数据管线和真实求职数据，不在这个仓库里，见 SKILL.md「数据从哪来」一节。）

> 版本约定：每次功能更新都会在本文件登记（新增 / 修复 / 文档三类），并在对应 commit message 中带版本号。

## v2.0 · 2026-09-24 发布（开发于 09-17）

### 新增

- **我的资质 + 硬性要求核对**：岗位池新增「我的资质」入口，可填写学历、专业、工作年限、政治面貌、证书等；页面按岗位的「学历要求」「专业要求」实时核对，标出「专业匹配」徽标（匹配 / 不符 / 未标明），并支持「只看匹配度≥60%」「隐藏硬性不符」两个筛选开关
- **JD 多岗位对比**：新增独立页面，支持粘贴多条完整 JD（用 `====` / `---` / `***` 分隔），自动抽取单位 / 岗位 / 城市 / 薪资 / 截止时间 / 核心要求，排成表格并排对比，可切换查看每条 JD 原文；只做抽取排版，不打分、不生成文案
- **岗位池新列**：薪资、学历要求、专业要求
- **岗位管理支持截图上传**：可为岗位添加截图，支持点击放大查看
- **顶部横向同步滚动条**：宽表格不用拖到页面底部即可左右滑动
- `jobs.json` 新增可选字段：`薪资` `专业要求` `学历要求` `经验要求` `招聘人数` `部门` `专业匹配` `投递状态` `投递日期`（全部可留空，不影响旧数据）

### 修复

- 岗位排序：发布时间在未来的岗位（常见于双选会/宣讲会日期误填）不再顶到最前面，改为沉底，避免真实最新岗位被挤下去

### 文档

- `README.md` / `SKILL.md` 补充上述新功能说明
- `素材库/jobs.json` 示例数据补齐新字段示例值
- `素材库/填表说明.md` 新增「我的资质」「JD 多岗位对比」使用说明和新字段释义

## v1.0 · 2026-09-01

- 首次发布：单文件 HTML 工作台（可视化看板 / 岗位池 / 岗位管理 / 经历库 / JD 对照 / 面试准备），内置示例数据，localStorage 持久化
