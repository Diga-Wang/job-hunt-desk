---
name: 个人信息台构建器
description: 一个已经做好的「秋招信息工作台」单文件 HTML——看板/岗位池/岗位管理/经历库/JD对照/面试六大面板，内置示例数据，双击即用、离线可用、数据不出本机。配素材库（jobs.json / experience.json 模板）让别人把自己的岗位与经历数据替换进去。触发场景：用户说「给我做一个秋招工作台」「别人下载就能用的求职信息台」「把我的工作台打包发出去」「开箱即用的求职看板」。
agent_created: true
---

# 秋招信息工作台（开箱即用版）

别人装上这个 skill，拿到的是一个**已经做好的**秋招信息工作台（单文件 HTML），
**不是"造台子的工具"**。双击就能开，内置示例数据，离线可用，数据不出本机。

## 里面有什么

`秋招信息工作台（共享版）.html` —— 一个多面板工作台：

- **看板**：左上角 KPI（岗位数、待投递、截止临近……）
- **岗位池**：岗位清单，可搜索 / 筛选 / 按截止时间排序
- **岗位管理**：状态流转、投递日期、备注、JD 原文拆解、关键词、经历库命中、我的判定
- **经历库**：只读速查区，读自 experience.json（优势 / 风险追问 / 教育背景 / 工具技能 / 能力标签 / 转型叙事 / 核实修正）
- **JD 对照**：粘贴 JD → 按行拆解 → 与经历库做关键词命中（判定权在你）
- **面试**：面经记录 + 基于经历库预填的准备清单

## 别人怎么用（三步）

1. **打开就用**：双击 `秋招信息工作台（共享版）.html`。
   内置 14 条示例岗位 + 3 条示例经历，立刻能看到完整效果。
2. **换成自己的数据**：把 `素材库/` 里的 `jobs.json`、`experience.json`
   换成你自己的内容（字段说明见 `素材库/填表说明.md`）。
3. **导入**：在页面里点「点击更新」（岗位池）/「导入 experience.json」（经历库），
   选中你改好的文件即可；也可直接在页面里增删改。

> 数据全程在浏览器本地（localStorage / 你选的文件），不上传、不联网取数。

## 素材库（别人把数据放这里）

`素材库/` 是给别人填自己数据的地方：

| 文件 | 作用 |
|---|---|
| `jobs.json` | 岗位数据。替换成你自己的岗位清单 |
| `experience.json` | 经历库数据。替换成你自己的经历档案 |
| `填表说明.md` | 两个 JSON 的字段含义 + 导入方式 |

## 数据从哪来（仅授权渠道）

本 skill 只**呈现**数据，不**获取**数据。真实岗位 / 经历要用你有权使用的渠道取得：
本地文件 · 表格 · 数据库导出 · 手工录入 · 对方明确开放的导出/接口。
**不做登录态绕过、不做签名复刻、不做频率规避。** 对方只给网页时，优先自己导出 CSV/Excel 再导入。

你私人的取数脚本（如国央企秋招采集器）留在本地项目，**不要**打进这个公开 skill。

## 注意

- 这个 skill 的成品是**已经做好的台子**，不需要 Python、不需要 build。
- 之前那套「访谈 → 素材库模板 → build_desk.py 生成」流程已归档在
  `_archive/造台子生成器(可选)/`，一般不用，除非你要从零定制一个完全不同的台子。
